# setup.py

from setuptools import setup, find_packages

setup(
    name='Math_Quiz',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        # List your dependencies here
    ],
)